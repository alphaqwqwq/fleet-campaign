# ADR-003 LLM 不进入规则闭环

- 状态：接受
- 日期：2026-08-13

## 背景

电子桌游的叙事层可能接入 LLM，但规则、随机数、结算、胜负不能由 LLM 决定。

## 决定

- LLM / 旁白是可删除、可降级的叙事表现层，不进规则闭环。
- 若实现 Hook，只写已确认事实 + 手写降级文本，不保存 API Key、不调用真实模型、不写回规则状态。
- `demo-v1` 首版无旁白运行时，仅保留契约。

## 后果

- 领域状态永不被叙事污染；随机数只由房主确定性消费。

## 备选

- LLM 参与结算：交付快但规则不可复现、不可审计，已否决。
